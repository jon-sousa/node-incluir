<%- include('./partials/beginOfPage') -%>

<h1 class="text-center py-3"><%= title %></h1>
<% for(let empresa of empresas){ %>
    <div class="card w-50 mx-auto mb-2 pointer border border-3" id= <%= empresa._id %> data-click=false onclick="expandir(this)">
        <div class="card-body mt-2">
            <h5 class="card-title"><%= empresa.nome %></h3>
            <h6 class="card-subtitle"><%= empresa.cnpj %></h4>
        </div>
    </div>
<% } %>

<%- include('./partials/endOfPage') -%>